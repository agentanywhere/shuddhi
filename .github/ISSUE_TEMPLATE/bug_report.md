---
name: Bug report
about: Something behaved incorrectly
labels: bug
---

**What happened**

**What you expected**

**Reproduce**
```bash
# the exact commands
```

**Environment** — paste the output of `python3 factory.py doctor`:
```
```

**Manifests** — if the problem involves a build, attach or paste
`run/<shard>.stats.json` and `build/BUILD-MANIFEST.json`. They record engine
version, library versions, and every threshold used.

> Receipt-integrity and provenance-gate bypass issues should go to
> security@shephertz.com instead — see SECURITY.md.
